# bootstrap_mac

Factory Restore macOS
`caffinate /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/purplejay-io/bootstrap_mac/master/restore.sh)"`

Baseline setup for Purple Jay macOS
`caffinate /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/purplejay-io/bootstrap_mac/master/setup.sh)"`

